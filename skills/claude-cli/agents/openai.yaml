interface:
  display_name: "Claude CLI"
  short_description: "Delegate tasks directly to Claude Code"
  default_prompt: "Use $claude-cli to delegate a scoped task directly to Claude Code without CAO."
