interface:
  display_name: "Orchestrator CLI"
  short_description: "Coordinate GitHub or offline Markdown tasks"
  default_prompt: "Use $orchestrator-cli to plan, dispatch, and track tasks through GitHub Issues or local Markdown when GitHub is unavailable."
